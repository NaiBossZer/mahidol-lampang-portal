import { getSupabaseUser, isAdminRole, json, supabaseConfig } from "../auth/_shared";type Env=Record<string,unknown>;const cookie=(r:Request)=>{const x=(r.headers.get("Cookie")??"").split(";").map(v=>v.trim()).find(v=>v.startsWith("sb_access_token="));return x?decodeURIComponent(x.slice(17)):null};
export async function onRequest({request,env}:{request:Request,env:Env}){try{const u=await getSupabaseUser(request,env),role=u?.app_metadata?.role,token=cookie(request);if(!u||!isAdminRole(role)||!token)return json({success:false,error:"Unauthorized"},401);const {url,key}=supabaseConfig(env),h={apikey:key,Authorization:`Bearer ${token}`,Accept:"application/json"};const [a,s,r]=await Promise.all([fetch(`${url}/rest/v1/activities?select=id,title,activity_date,status,participant_count`,{headers:h}),fetch(`${url}/rest/v1/survey_responses?select=id,activity_id,submitted_at,affiliation,feedback,p2_location,p2_schedule,p2_readiness,p2_reception,p2_overall,p3_interest,p3_content,p3_clarity,p3_benefit,p3_application,p4_knowledge,p4_inspiration,p4_community_resource,p4_future_return`,{headers:h}),fetch(`${url}/rest/v1/learning_centers?select=id,name,lifecycle_status`,{headers:h})]);if(!a.ok||!s.ok||!r.ok)return json({success:false,error:"ไม่สามารถสร้างรายงานจากข้อมูลกลางได้"},500);const activities=await a.json(),responses=await s.json(),centers=await r.json();const validActivities=activities.filter((x:any)=>!['cancelled','archived','retired'].includes(x.status));const scores=responses.flatMap((x:any)=>Object.entries(x).filter(([k,v])=>/^p[234]_/.test(k)&&typeof v==='number').map(([,v])=>v as number));const average=scores.length?scores.reduce((a:number,b:number)=>a+b,0)/scores.length:null;return json({success:true,data:{generatedAt:new Date().toISOString(),activities:validActivities.length,learningCenters:centers.filter((x:any)=>x.lifecycle_status==='active').length,responses:responses.length,averageScore:average?Number(average.toFixed(2)):null,satisfaction:average?Number(((average/5)*100).toFixed(2)):null}})}catch(e){console.error("/api/admin/reports",e);return json({success:false,error:e instanceof Error?e.message:"Reports failed"},500)}}