import {getSupabaseUser,isAdminRole,json,supabaseConfig} from "../auth/_shared";type Env=Record<string,unknown>;const cookie=(r:Request)=>{const x=(r.headers.get("Cookie")??"").split(";").map(v=>v.trim()).find(v=>v.startsWith("sb_access_token="));return x?decodeURIComponent(x.slice(17)):null};
export async function onRequest({request,env}:{request:Request,env:Env}){try{const u=await getSupabaseUser(request,env),role=u?.app_metadata?.role,t=cookie(request);if(!u||!isAdminRole(role)||!t)return json({success:false,error:"Unauthorized"},401);const {url,key}=supabaseConfig(env),h={apikey:key,Authorization:`Bearer ${t}`,Accept:"application/json","Content-Type":"application/json"};const p=new URL(request.url).searchParams;if(request.method==="GET"){const r=await fetch(`${url}/rest/v1/lifecycle_events?select=*&order=created_at.desc&limit=250`,{headers:h});return json({success:r.ok,data:r.ok?await r.json():null},r.ok?200:r.status)}if(!["SUPER_ADMIN","CONTENT_ADMIN","OPERATIONS_ADMIN"].includes(String(role)))return json({success:false,error:"Forbidden"},403);if(request.method!=="PATCH")return json({success:false,error:"Method Not Allowed"},405,{Allow:"GET, PATCH"});const type=p.get("type"),id=p.get("id"),status=p.get("status");const table=type==="activity"?"activities":type==="occurrence"?"activity_occurrences":type==="learning_center"?"learning_centers":type==="organization"?"organizations":null;if(!table||!id||!status)return json({success:false,error:"type, id, status required"},400);const r=await fetch(`${url}/rest/v1/${table}?id=eq.${encodeURIComponent(id)}`,{method:"PATCH",headers:{...h,Prefer:"return=representation"},body:JSON.stringify(type==="learning_center"?{lifecycle_status:status,updated_at:new Date().toISOString()}:{status,updated_at:new Date().toISOString()})});const b=await r.json().catch(()=>null);return json({success:r.ok,data:b},r.ok?200:r.status)}catch(e){console.error("/api/admin/lifecycle",e);return json({success:false,error:e instanceof Error?e.message:"Lifecycle failed"},500)}}