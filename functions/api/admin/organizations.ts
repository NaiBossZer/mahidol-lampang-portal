import { getSupabaseUser, isAdminRole, json, supabaseConfig } from "../auth/_shared";
type Env=Record<string,unknown>; type Row=Record<string,unknown>;
function token(r:Request){const p=(r.headers.get("Cookie")??"").split(";").map(x=>x.trim()).find(x=>x.startsWith("sb_access_token="));return p?decodeURIComponent(x=>x.slice(17)):null;}
async function sb(env:Env,t:string,path:string,init:RequestInit={}){const {url,key,configured}=supabaseConfig(env);if(!configured)throw new Error("Supabase is not configured");const r=await fetch(`${url}/rest/v1/${path}`,{...init,headers:{apikey:key,Authorization:`Bearer ${t}`,Accept:"application/json","Content-Type":"application/json",...(init.headers??{})}});const b=await r.json().catch(()=>null);if(!r.ok)throw new Error(`Supabase REST ${r.status}`);return b;}
export async function onRequest({request,env}:{request:Request;env:Env}){const u=await getSupabaseUser(request,env),r=u?.app_metadata?.role,t=token(request);if(!u||!isAdminRole(r)||!t)return json({success:false,error:"Unauthorized"},401);if(request.method!=="GET"&&!["SUPER_ADMIN","OPERATIONS_ADMIN"].includes(String(r)))return json({success:false,error:"Forbidden"},403);try{if(request.method==="GET")return json({success:true,data:await sb(env,t,"organizations?select=*&order=name.asc")});if(request.method==="POST"){const b=await request.json();return json({success:true,data:(await sb(env,t,"organizations",{method:"POST",headers:{Prefer:"return=representation"},body:JSON.stringify(b)}))[0]},201);}if(request.method==="PUT"){const id=new URL(request.url).searchParams.get("id");if(!id)return json({success:false,error:"id required"},400);const b=await request.json();return json({success:true,data:(await sb(env,t,`organizations?id=eq.${encodeURIComponent(id)}`,{method:"PATCH",headers:{Prefer:"return=representation"},body:JSON.stringify(b)}))[0]});}return json({success:false,error:"Method Not Allowed"},405);}catch(e){console.error("/api/admin/organizations",e);return json({success:false,error:e instanceof Error?e.message:"ไม่สามารถจัดการองค์กรได้"},500);}}
