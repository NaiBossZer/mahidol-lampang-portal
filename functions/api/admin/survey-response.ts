import { getSupabaseUser, isAdminRole, json, supabaseConfig } from "../auth/_shared";type Env=Record<string,unknown>;const cookie=(r:Request)=>{const x=(r.headers.get("Cookie")??"").split(";").map(v=>v.trim()).find(v=>v.startsWith("sb_access_token="));return x?decodeURIComponent(x.slice(17)):null};
export async function onRequest({request,env}:{request:Request,env:Env}){try{const u=await getSupabaseUser(request,env),role=u?.app_metadata?.role,token=cookie(request);if(!u||!isAdminRole(role)||!token)return json({success:false,error:"Unauthorized"},401);const id=new URL(request.url).searchParams.get("id");if(!id)return json({success:false,error:"id required"},400);const {url,key}=supabaseConfig(env),h={apikey:key,Authorization:`Bearer ${token}`,Accept:"application/json"};const [r,a,q]=await Promise.all([fetch(`${url}/rest/v1/survey_responses?id=eq.${encodeURIComponent(id)}&select=*`,{headers:h}),fetch(`${url}/rest/v1/survey_answers?response_id=eq.${encodeURIComponent(id)}&select=id,question_id,answer_number,answer_text,answer_options,created_at&order=created_at.asc`,{headers:h}),fetch(`${url}/rest/v1/survey_questions?select=id,section_key,question_text,question_type,required,order_index,options,scale_min,scale_max&id=in.(${encodeURIComponent("00000000-0000-0000-0000-000000000000")})`,{headers:h})]);if(!r.ok||!a.ok)return json({success:false,error:"ไม่สามารถโหลดคำตอบได้"},500);const response=(await r.json())[0]??null;if(!response)return json({success:false,error:"ไม่พบคำตอบ"},404);const answers=await a.json();const ids=answers.map((x:any)=>x.question_id).filter(Boolean);let questions:any[]=[];if(ids.length){const qr=await fetch(`${url}/rest/v1/survey_questions?select=id,section_key,question_text,question_type,required,order_index,options,scale_min,scale_max&id=in.(${ids.join(",")})&order=order_index.asc`,{headers:h});if(qr.ok)questions=await qr.json();}return json({success:true,data:{response,answers,questions}})}catch(e){console.error("/api/admin/survey-response",e);return json({success:false,error:e instanceof Error?e.message:"Response detail failed"},500)}}