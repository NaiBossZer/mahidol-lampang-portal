import { getSupabaseUser, isAdminRole, json, supabaseConfig } from "../auth/_shared";
type Env = Record<string, unknown>;
function cookie(request: Request) { const part = (request.headers.get("Cookie") ?? "").split(";").map((value) => value.trim()).find((value) => value.startsWith("sb_access_token=")); return part ? decodeURIComponent(part.slice("sb_access_token=".length)) : null; }
async function rest(env: Env, token: string, path: string, init: RequestInit = {}) { const config = supabaseConfig(env); if (!config.configured) throw new Error("Supabase is not configured"); const response = await fetch(`${config.url}/rest/v1/${path}`, { ...init, headers: { apikey: config.key, Authorization: `Bearer ${token}`, Accept: "application/json", ...(init.headers ?? {}) } }); const body = await response.json().catch(() => null); if (!response.ok) throw new Error(`Supabase REST ${response.status}`); return body; }
export async function onRequest({ request, env }: { request: Request; env: Env }) { try { const user = await getSupabaseUser(request, env); const role = String(user?.app_metadata?.role ?? ""); const token = cookie(request); if (!user || !isAdminRole(role) || !token) return json({ success: false, error: "Unauthorized" }, 401); if (request.method !== "GET" && !["SUPER_ADMIN", "CONTENT_ADMIN", "OPERATIONS_ADMIN"].includes(role)) return json({ success: false, error: "Forbidden" }, 403); if (request.method === "GET") { const params = new URL(request.url).searchParams; const query = new URLSearchParams({ select: "id,activity_id,occurrence_id,storage_path,public_url,media_type,caption,is_post_event,display_order,status,created_at", order: "display_order.asc,created_at.desc" }); if (params.get("activityId")) query.set("activity_id", `eq.${params.get("activityId")}`); return json({ success: true, data: await rest(env, token, `activity_media?${query.toString()}`) }); }
  if (request.method !== "POST") return json({ success: false, error: "Method Not Allowed" }, 405, { Allow: "GET, POST" }); const form = await request.formData(); const activityId = String(form.get("activityId") ?? ""); const occurrenceId = String(form.get("occurrenceId") ?? "") || null; const caption = String(form.get("caption") ?? "") || null; const file = form.get("file"); if (!activityId || !(file instanceof File)) return json({ success: false, error: "activityId และ file จำเป็น" }, 400); if (!["image/jpeg", "image/png", "image/webp"].includes(file.type)) return json({ success: false, error: "รองรับเฉพาะ JPEG, PNG, WebP" }, 415); if (file.size > 10 * 1024 * 1024) return json({ success: false, error: "ไฟล์ต้องไม่เกิน 10MB" }, 413); const safeName = file.name.replace(/[^a-zA-Z0-9._-]/g, "-"); const path = `activities/${activityId}/${crypto.randomUUID()}-${safeName}`; const config = supabaseConfig(env); const upload = await fetch(`${config.url}/storage/v1/object/activity-media/${path}`, { method: "POST", headers: { apikey: config.key, Authorization: `Bearer ${token}`, "Content-Type": file.type, "x-upsert": "false" }, body: await file.arrayBuffer() }); if (!upload.ok) throw new Error(`Storage upload ${upload.status}`); const publicUrl = `${config.url}/storage/v1/object/public/activity-media/${path}`; const row = await rest(env, token, "activity_media", { method: "POST", headers: { "Content-Type": "application/json", Prefer: "return=representation" }, body: JSON.stringify({ activity_id: activityId, occurrence_id: occurrenceId, storage_path: path, public_url: publicUrl, media_type: "image", caption, is_post_event: true, created_by: user.id }) }); return json({ success: true, data: Array.isArray(row) ? row[0] : row }, 201); } catch (error) { console.error("/api/admin/activity-media", error); return json({ success: false, error: error instanceof Error ? error.message : "Media operation failed" }, 500); } }
