import { getSupabaseUser, isAdminRole, json, supabaseConfig } from "../auth/_shared";type Env=Record<string,unknown>;const cookie=(r:Request)=>{const x=(r.headers.get("Cookie")??"").split(";").map(v=>v.trim()).find(v=>v.startsWith("sb_access_token="));return x?decodeURIComponent(x.slice(17)):null};async function rpc(env:Env,token:string,body:unknown){const {url,key}=supabaseConfig(env);const r=await fetch(`${url}/rest/v1/rpc/set_central_admin_role`,{method:"POST",headers:{apikey:key,Authorization:`Bearer ${token}`,"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify(body)});const b=await r.json().catch(()=>null);if(!r.ok)throw new Error(`Role update ${r.status}`);return b}
export async function onRequest({request,env}:{request:Request,env:Env}){try{const u=await getSupabaseUser(request,env),role=u?.app_metadata?.role,token=cookie(request);if(!u||!isAdminRole(role)||!token)return json({success:false,error:"Unauthorized"},401);const {url,key}=supabaseConfig(env);const headers={apikey:key,Authorization:`Bearer ${token}`,Accept:"application/json"};if(request.method==="GET"){const r=await fetch(`${url}/rest/v1/staff_profiles?select=user_id,personnel_id,full_name,position,department,role,active,central_role,created_at,updated_at&order=full_name.asc`,{headers});return json({success:r.ok,data:r.ok?await r.json():null},r.ok?200:r.status);}if(request.method==="PATCH"){if(role!=="SUPER_ADMIN")return json({success:false,error:"Forbidden"},403);const body=await request.json() as {userId?:string;role?:string};if(!body.userId||!body.role)return json({success:false,error:"userId and role required"},400);return json({success:true,data:await rpc(env,token,{target_user_id:body.userId,new_role:body.role})});}return json({success:false,error:"Method Not Allowed"},405,{Allow:"GET, PATCH"});}catch(e){console.error("/api/admin/users",e);return json({success:false,error:e instanceof Error?e.message:"Admin users failed"},500)}}