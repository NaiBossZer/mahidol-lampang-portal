import { authorize, clean, idFromUrl, methodNotAllowed, nullable, rest } from "./_cms";
import { json } from "../auth/_shared";
type Env = Record<string, unknown>; type Row = Record<string, unknown>;
export async function onRequest({ request, env }: { request: Request; env: Env }) { const permission = request.method === "GET" ? "navigation.read" : request.method === "POST" ? "navigation.create" : "navigation.update"; const auth = await authorize(request, env, permission); if ("error" in auth) return auth.error; try { if (request.method === "GET") return json({ success: true, data: await rest(env, auth.token, "navigation_items?select=*&order=sort_order.asc") }); const id = idFromUrl(request); const body = await request.json() as Record<string, unknown>; const label = clean(body.label, 255); const slug = clean(body.slug, 255); if (!label || !slug) return json({ success: false, error: "กรุณาระบุ label และ slug" }, 400); const row = { label, slug, parent_id: nullable(body.parentId), target_type: clean(body.targetType, 30) || "INTERNAL", target_url: nullable(body.targetUrl), sort_order: Number.isInteger(Number(body.sortOrder)) ? Number(body.sortOrder) : 0, is_enabled: body.isEnabled !== false, open_new_tab: body.openNewTab === true, updated_at: new Date().toISOString() }; if (request.method === "POST") { const rows = await rest(env, auth.token, "navigation_items", { method: "POST", headers: { Prefer: "return=representation" }, body: JSON.stringify(row) }) as Row[]; return json({ success: true, data: rows[0] ?? null }, 201); } if (!["PUT", "PATCH"].includes(request.method) || !id) return methodNotAllowed(["GET", "POST", "PUT", "PATCH"]); const rows = await rest(env, auth.token, `navigation_items?id=eq.${id}`, { method: "PATCH", headers: { Prefer: "return=representation" }, body: JSON.stringify(row) }) as Row[]; if (!rows[0]) return json({ success: false, error: "ไม่พบเมนู" }, 404); return json({ success: true, data: rows[0] }); } catch (error) { console.error("/api/admin/navigation", error); return json({ success: false, error: error instanceof Error ? error.message : "ไม่สามารถจัดการเมนูได้" }, 500); } }
