import { useEffect,useState } from "react";import { ImagePlus,RefreshCw } from "lucide-react";import { toast } from "sonner";
type Media={id:string;activity_id:string;public_url?:string;caption?:string|null;storage_path:string;created_at:string};export function ActivityMediaPage(){const[activityId,setActivityId]=useState("");const[activities,setActivities]=useState<any[]>([]);const[media,setMedia]=useState<Media[]>([]);const[file,setFile]=useState<File|null>(null);const[loading,setLoading]=useState(true);async function loadActivities(){const r=await fetch("/api/admin/activities",{credentials:"include",headers:{Accept:"application/json"}});const b=await r.json();if(!r.ok)throw new Error(b?.error||"โหลดกิจกรรมไม่สำเร็จ");setActivities(b.data??[])}async function loadMedia(id:string){if(!id){setMedia([]);return}const r=await fetch(`/api/admin/activity-media?activityId=${encodeURIComponent(id)}`,{credentials:"include",headers:{Accept:"application/json"}});const b=await r.json();if(!r.ok)throw new Error(b?.error||"โหลดภาพไม่สำเร็จ");setMedia(b.data??[])}async function load(){setLoading(true);try{await loadActivities();await loadMedia(activityId)}catch(e){toast.error(e instanceof Error?e.message:"โหลดไม่สำเร็จ")}finally{setLoading(false)}}useEffect(()=>{void load()},[]);useEffect(()=>{void loadMedia(activityId)},[activityId]);async function upload(){if(!activityId||!file)return toast.error("เลือกกิจกรรมและไฟล์ภาพก่อน");const f=new FormData();f.set("activityId",activityId);f.set("file",file);try{const r=await fetch("/api/admin/activity-media",{method:"POST",credentials:"include",body:f});const b=await r.json();if(!r.ok)throw new Error(b?.error||"อัปโหลดไม่สำเร็จ");setFile(null);toast.success("อัปโหลดภาพแล้ว");await loadMedia(activityId)}catch(e){toast.error(e instanceof Error?e.message:"อัปโหลดไม่สำเร็จ")}}return <section className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8"><div className="flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-emerald-700">Activity Photos</p><h1 className="mt-1 text-2xl font-bold text-brand-navy lg:text-3xl">ภาพกิจกรรมหลังเสร็จสิ้นกิจกรรม</h1></div><button type="button" onClick={()=>void load()} className="inline-flex min-h-11 items-center gap-2 rounded-xl border bg-white px-4 text-sm font-semibold"><RefreshCw className="h-4 w-4"/>รีเฟรช</button></div><div className="mt-6 rounded-2xl border bg-white p-5 shadow-sm"><div className="grid gap-3 md:grid-cols-[1fr_1fr_auto]"><select value={activityId} onChange={e=>setActivityId(e.target.value)} className="dashboard-control"><option value="">เลือกกิจกรรม</option>{activities.map(a=><option key={a.id} value={a.id}>{a.title}</option>)}</select><input type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>setFile(e.target.files?.[0]??null)} className="dashboard-control"/><button type="button" onClick={()=>void upload()} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-brand-navy px-4 text-sm font-bold text-white"><ImagePlus className="h-4 w-4"/>อัปโหลด</button></div></div>{loading?<div className="mt-6 p-8 text-center text-sm text-slate-500">กำลังโหลด...</div>:<div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{media.map(m=><figure key={m.id} className="overflow-hidden rounded-2xl border bg-white shadow-sm"><img src={m.public_url} alt={m.caption??"ภาพกิจกรรม"} className="aspect-[4/3] w-full object-cover"/><figcaption className="p-3 text-xs text-slate-500">{m.caption??m.storage_path}</figcaption></figure>)}{media.length===0&&<p className="text-sm text-slate-500">ยังไม่มีภาพสำหรับกิจกรรมนี้</p>}</div>}</section>}
