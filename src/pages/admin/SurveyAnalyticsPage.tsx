import {useEffect,useState} from "react";import {BarChart3,RefreshCw,Users} from "lucide-react";import {Link} from "react-router-dom";import {toast} from "sonner";type Data={responses:Array<Record<string,unknown>>;summary:{respondents:number;answeredScores:number;averageScore:number|null;satisfactionPercent:number|null}};export function SurveyAnalyticsPage(){const[data,setData]=useState<Data|null>(null),[loading,setLoading]=useState(true);async function load(){setLoading(true);try{const r=await fetch("/api/admin/survey-responses",{credentials:"include",headers:{Accept:"application/json"}}),b=await r.json();if(!r.ok)throw new Error(b.error||"โหลดผลประเมินไม่สำเร็จ");setData(b.data)}catch(e){toast.error(e instanceof Error?e.message:"โหลดผลประเมินไม่สำเร็จ")}finally{setLoading(false)}}useEffect(()=>{void load()},[]);return <section className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-emerald-700">Survey Analytics</p><h1 className="mt-1 text-2xl font-bold text-brand-navy lg:text-3xl">ผลการประเมิน</h1><p className="mt-1 text-sm text-slate-600">อ่านผลจากคำตอบจริงในฐานข้อมูลกลาง โดยไม่ใช้ Excel เป็นแหล่งข้อมูลหลัก</p></div><button type="button" onClick={()=>void load()} className="inline-flex min-h-11 items-center gap-2 rounded-xl border bg-white px-4 text-sm font-semibold text-brand-navy"><RefreshCw className="h-4 w-4"/>รีเฟรช</button></div>{loading?<div className="mt-6 rounded-2xl border bg-white p-10 text-center text-sm text-slate-500">กำลังโหลด...</div>:data&&<><div className="mt-6 grid gap-4 sm:grid-cols-3">{[[Users,"ผู้ตอบแบบสอบถาม",data.summary.respondents],[BarChart3,"คะแนนเฉลี่ย",data.summary.averageScore==null?"—":data.summary.averageScore.toFixed(2)],[BarChart3,"ความพึงพอใจ",data.summary.satisfactionPercent==null?"—":`${data.summary.satisfactionPercent}%`]].map(([I,l,v])=>{const Icon=I as typeof BarChart3;return <div key={String(l)} className="rounded-2xl border bg-white p-5 shadow-sm"><Icon className="h-5 w-5 text-emerald-700"/><p className="mt-4 text-xs text-slate-500">{String(l)}</p><p className="text-3xl font-bold text-brand-navy">{String(v)}</p></div>})}</div><div className="mt-5 rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold text-brand-navy">รายการคำตอบ</h2>{data.responses.length===0?<p className="mt-6 text-sm text-slate-500">ยังไม่มีคำตอบ</p>:<div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b text-xs text-slate-500"><tr><th className="p-3">Submitted</th><th className="p-3">Activity</th><th className="p-3">Affiliation</th><th className="p-3">Feedback</th><th className="p-3">Detail</th></tr></thead><tbody>{data.responses.map(r=><tr key={String(r.id)} className="border-b border-slate-100"><td className="p-3 whitespace-nowrap">{new Date(String(r.submitted_at)).toLocaleString("th-TH")}</td><td className="p-3">{String(r.activity_id)}</td><td className="p-3">{String(r.affiliation||"—")}</td><td className="p-3 max-w-md">{String(r.feedback||"—")}</td><td className="p-3"><Link to={`/admin/surveys/response?id=${encodeURIComponent(String(r.id))}`} className="font-semibold text-brand-navy underline">ดูรายละเอียด</Link></td></tr>)}</tbody></table></div>}</div></>}</section>}
