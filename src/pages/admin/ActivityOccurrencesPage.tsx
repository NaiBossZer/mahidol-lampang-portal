import { useEffect, useState } from "react";
import { CalendarDays, Plus, RefreshCw, Archive, X } from "lucide-react";
import { toast } from "sonner";
import { getAdminActivities, type AdminActivity } from "@/services/api";
import { archiveAdminOccurrence, createAdminOccurrence, getAdminOccurrences, type ActivityOccurrence } from "@/services/admin-occurrences";

const blank = { activityId: "", occurrenceNo: 1, startAt: "", endAt: "", status: "scheduled" as ActivityOccurrence["status"], participant_count: 0, location_type: "center" as NonNullable<ActivityOccurrence["location_type"]>, location_detail: "" };
export function ActivityOccurrencesPage() {
  const [activities, setActivities] = useState<AdminActivity[]>([]);
  const [activityId, setActivityId] = useState("");
  const [items, setItems] = useState<ActivityOccurrence[]>([]);
  const [open, setOpen] = useState(false);
  const [form, setForm] = useState(blank);
  const [loading, setLoading] = useState(true);
  async function load() { setLoading(true); try { const [a] = await Promise.all([getAdminActivities()]); setActivities(a); const selected = activityId || a[0]?.id || ""; if (!activityId) setActivityId(selected); setItems(await getAdminOccurrences(selected)); } catch (e) { toast.error(e instanceof Error ? e.message : "โหลดข้อมูลไม่สำเร็จ"); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  useEffect(() => { if (activityId) void getAdminOccurrences(activityId).then(setItems).catch(() => toast.error("โหลดรอบกิจกรรมไม่สำเร็จ")); }, [activityId]);
  function add() { const next = items.filter((x) => x.status !== "cancelled" && x.status !== "archived").length + 1; setForm({ ...blank, activityId, occurrenceNo: next }); setOpen(true); }
  async function save() { if (!form.activityId || !form.startAt) { toast.error("กรุณาระบุกิจกรรมและเวลาเริ่ม"); return; } try { await createAdminOccurrence(form); toast.success("สร้างรอบกิจกรรมแล้ว"); setOpen(false); setItems(await getAdminOccurrences(activityId)); } catch (e) { toast.error(e instanceof Error ? e.message : "บันทึกไม่สำเร็จ"); } }
  async function archive(id: string) { try { await archiveAdminOccurrence(id); toast.success("เก็บรอบกิจกรรมแล้ว"); setItems(await getAdminOccurrences(activityId)); } catch (e) { toast.error(e instanceof Error ? e.message : "ดำเนินการไม่สำเร็จ"); } }
  const selectedName = activities.find((a) => a.id === activityId)?.title ?? "กิจกรรม";
  return <section className="mx-auto max-w-6xl px-4 py-6 sm:px-6 lg:px-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.14em] text-emerald-700">Occurrence Management</p><h1 className="mt-1 text-2xl font-bold text-brand-navy lg:text-3xl">รอบการจัดกิจกรรม</h1><p className="mt-1 text-sm text-slate-600">จัดการการจัดกิจกรรมแต่ละครั้ง โดยไม่สร้าง Activity ซ้ำ</p></div><div className="flex gap-2"><button type="button" onClick={() => void load()} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 text-sm font-semibold text-brand-navy"><RefreshCw className="h-4 w-4"/>รีเฟรช</button><button type="button" onClick={add} disabled={!activityId} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-brand-navy px-4 text-sm font-bold text-white disabled:opacity-50"><Plus className="h-4 w-4"/>เพิ่มรอบ</button></div></div><div className="mt-6 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><label className="block text-sm font-semibold text-slate-700">Activity<select value={activityId} onChange={(e) => setActivityId(e.target.value)} className="dashboard-control mt-1 w-full">{activities.map((a) => <option key={a.id} value={a.id}>{a.title}</option>)}</select></label></div><div className="mt-4 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{loading ? <div className="p-10 text-center text-sm text-slate-500">กำลังโหลด...</div> : items.filter((x) => !["cancelled", "archived"].includes(x.status)).length === 0 ? <div className="p-10 text-center text-sm text-slate-500">ยังไม่มีรอบการจัดกิจกรรมสำหรับ {selectedName}</div> : <div className="divide-y divide-slate-100">{items.filter((x) => !["cancelled", "archived"].includes(x.status)).map((x) => <div key={x.id} className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold text-slate-600">ครั้งที่ {x.occurrence_no}</span><span className="text-xs font-semibold text-emerald-700">{x.status}</span></div><p className="mt-2 flex items-center gap-2 font-semibold text-slate-900"><CalendarDays className="h-4 w-4 text-slate-400"/>{new Date(x.start_at).toLocaleString("th-TH")}</p><p className="mt-1 text-sm text-slate-500">{x.location_detail || "ไม่ระบุสถานที่"} · ผู้เข้าร่วม {x.participant_count}</p></div><button type="button" onClick={() => void archive(x.id)} className="inline-flex min-h-10 items-center justify-center gap-2 rounded-xl border border-slate-200 px-3 text-sm font-semibold text-slate-600"><Archive className="h-4 w-4"/>เก็บ</button></div>)}</div>}</div>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><div className="w-full max-w-2xl rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-slate-200 p-5"><h2 className="text-lg font-bold text-brand-navy">เพิ่มรอบการจัดกิจกรรม</h2><button type="button" onClick={() => setOpen(false)} aria-label="ปิด"><X className="h-5 w-5"/></button></div><div className="grid gap-4 p-5 sm:grid-cols-2"><label className="text-sm font-semibold">ครั้งที่<input type="number" min="1" value={form.occurrenceNo} onChange={(e) => setForm({ ...form, occurrenceNo: Number(e.target.value) })} className="dashboard-control mt-1 w-full"/></label><label className="text-sm font-semibold">เริ่ม<input type="datetime-local" value={form.startAt} onChange={(e) => setForm({ ...form, startAt: e.target.value })} className="dashboard-control mt-1 w-full"/></label><label className="text-sm font-semibold">สิ้นสุด<input type="datetime-local" value={form.endAt} onChange={(e) => setForm({ ...form, endAt: e.target.value })} className="dashboard-control mt-1 w-full"/></label><label className="text-sm font-semibold">ผู้เข้าร่วม<input type="number" min="0" value={form.participant_count} onChange={(e) => setForm({ ...form, participant_count: Number(e.target.value) })} className="dashboard-control mt-1 w-full"/></label><label className="text-sm font-semibold">รูปแบบสถานที่<select value={form.location_type} onChange={(e) => setForm({ ...form, location_type: e.target.value as typeof form.location_type })} className="dashboard-control mt-1 w-full"><option value="center">Center</option><option value="learning_center">Learning Center</option><option value="external">External</option><option value="online">Online</option><option value="hybrid">Hybrid</option></select></label><label className="text-sm font-semibold">รายละเอียดสถานที่<input value={form.location_detail} onChange={(e) => setForm({ ...form, location_detail: e.target.value })} className="dashboard-control mt-1 w-full"/></label></div><div className="flex justify-end gap-2 border-t border-slate-200 p-5"><button type="button" onClick={() => setOpen(false)} className="min-h-11 rounded-xl border border-slate-200 px-4 text-sm font-semibold">ยกเลิก</button><button type="button" onClick={() => void save()} className="min-h-11 rounded-xl bg-brand-navy px-5 text-sm font-bold text-white">บันทึก</button></div></div></div>}</section>;
}
