import {useEffect,useState} from "react";import {RefreshCw,ShieldCheck} from "lucide-react";import {AIAccessGuard} from "@/components/ai/AIAccessGuard";type Row={id:string;intent:string;status:string;risk_level:string;created_at:string;started_at?:string|null};export function AIExecutionPage(){const[data,setData]=useState<Row[]>([]);const[loading,setLoading]=useState(true);async function load(){setLoading(true);try{const r=await fetch("/api/admin/ai-queue",{credentials:"include",headers:{Accept:"application/json"}}),b=await r.json();if(!r.ok)throw new Error(b?.error||"โหลด execution ไม่สำเร็จ");setData(b.data??[])}catch(e){console.error(e)}finally{setLoading(false)}}useEffect(()=>{void load()},[]);const x=data[0];return <AIAccessGuard permission="ai.execution.read"><section className="min-h-[calc(100vh-4rem)] bg-[#f8f9ff] px-4 py-5 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1280px]"><div className="flex items-end justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-emerald-700">LIVE EXECUTION</p><h1 className="mt-1 text-2xl font-bold text-[#002d62] sm:text-[28px]">แผนและการทำงานของ AI</h1><p className="mt-1 text-sm text-slate-600">สถานะจาก execution จริงของ Portal</p></div><button type="button" onClick={()=>void load()} className="inline-flex h-10 items-center gap-2 rounded-xl border bg-white px-3 text-sm font-semibold"><RefreshCw className="h-4 w-4"/>รีเฟรช</button></div>{loading?<div className="mt-6 rounded-2xl border bg-white p-10 text-center text-sm text-slate-500">กำลังโหลด...</div>:x?<div className="mt-6 grid gap-5 lg:grid-cols-[1.3fr_.7fr]"><div className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs text-slate-400">CURRENT EXECUTION</p><h2 className="mt-1 font-bold text-brand-navy">{x.intent}</h2></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{x.status}</span></div><div className="mt-6 space-y-3">{["Intent","Context","Permission","Tool execution","Verification"].map((s,i)=><div key={s} className="flex items-center gap-3 rounded-xl border p-3"><span className="grid h-7 w-7 place-items-center rounded-full bg-slate-100 text-xs font-bold">{i+1}</span><span className="text-sm font-semibold">{s}</span></div>)}</div></div><div className="rounded-2xl border bg-white p-5 shadow-sm"><ShieldCheck className="h-6 w-6 text-emerald-700"/><h2 className="mt-3 font-bold text-brand-navy">Execution boundary</h2><div className="mt-4 space-y-2 text-xs text-slate-600"><p>Auth + AdminGuard + RBAC</p><p>AI Tool Registry</p><p>Portal API</p><p>Supabase / Storage</p><p>Audit linkage</p></div></div></div>:<div className="mt-6 rounded-2xl border border-dashed bg-white p-10 text-center text-sm text-slate-500">ยังไม่มี execution ที่กำลังทำงาน</div>}</div></section></AIAccessGuard>}
