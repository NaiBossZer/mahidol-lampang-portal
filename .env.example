# Server-only secrets. Never commit real values.
# Supabase Auth
SUPABASE_URL=https://[PROJECT-REF].supabase.co
SUPABASE_ANON_KEY=replace-with-supabase-anon-key
# Used only by the synchronous server-side JWT authorization helper when the project uses HS256 JWTs.
SUPABASE_JWT_SECRET=replace-with-supabase-jwt-secret
# Supabase PostgreSQL connection string.
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@[POOLER-HOST]:5432/postgres

# Legacy ADMIN_PASSWORD is no longer used for authentication after Phase 7.4.
# ADMIN_PASSWORD=deprecated

# Optional public configuration
# Google Drive Picker (public browser config; restrict the API key by domain/API)
VITE_GOOGLE_CLIENT_ID=replace-with-google-oauth-client-id
VITE_GOOGLE_API_KEY=replace-with-restricted-google-api-key

VITE_MODEL_URL=https://example.com/site-map.glb
EV_CHARGER_COUNT=2

# AI Configuration
GEMINI_API_KEY=replace-with-gemini-api-key
GEMINI_MODEL=gemini-3.8-flash
GEMINI_MAX_OUTPUT_TOKENS=500
GEMINI_TEMPERATURE=0.3
AI_RATE_LIMIT_ENABLED=true
AI_RATE_LIMIT_RPM=10
AI_COST_MONITORING_ENABLED=true
AI_BUDGET_LIMIT=100
