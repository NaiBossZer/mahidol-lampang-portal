# Prototype — Unified AI + Survey Workflow

Target UX: Documents → Analyze → Build → Review → Publish → Results.

The prototype introduces a reusable workflow shell with a 6-step stepper and three-column Context / Workspace / AI Assistant layout. It does not replace the existing Survey or AI backend.
