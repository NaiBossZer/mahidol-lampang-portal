# Executive Dashboard Required Layout

Implementation target from the approved reference screen.

1. Page Header: `Executive Dashboard` only.
2. Global Filter: one `Activity Mode` tab with live selectors below it.
3. Activity Context / Hero: approved reference structure.
4. KPI Summary: five KPI cards.
5. Analytics: three columns in the approved reading order.
6. Natural vertical scrolling; no fixed-height clipping.
7. Participant and response counts remain distinct; response-rate display is capped at 100% and mismatches are surfaced.
8. Sidebar, Homepage, API/data model, and backend business logic remain unchanged.
